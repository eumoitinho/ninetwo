# Twenty UI

This library was generated with [Nx](https://nx.dev).

## Building

Run `npx nx build ninetwo-ui` to build the library.

## Storybook Server

Run `npx nx start ninetwo-ui` to start the storybook development server on `localhost:6006`.

## Running unit tests

Run `npx nx test ninetwo-ui` to execute the unit tests via [Jest](https://jestjs.io).
